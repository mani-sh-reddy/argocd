apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-audiobooks
  namespace: apps
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  storageClassName: longhorn

