apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: whoami-ip
  namespace: whoami
spec:
  entryPoints:
  - web
  routes:
  - match: Host(`10.10.10.10`)
    kind: Rule
    services:
    - name: whoami
      port: 80